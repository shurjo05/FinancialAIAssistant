# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9        # required for file uploads

# --- Database ---
SQLAlchemy>=2.0,<3.0
alembic>=1.13          # schema migrations
psycopg[binary]>=3.1   # PostgreSQL driver (postgresql+psycopg://); [binary] = prebuilt wheels

# --- Data pipeline ---
pandas>=2.2,<3.0
numpy>=1.26,<3.0
python-dateutil>=2.9          # robust date parsing fallback

# --- Config ---
pydantic-settings>=2.0,<3.0

# --- Auth ---
bcrypt>=4.0                # password hashing
python-jose[cryptography]>=3.3  # JWT create/verify
email-validator>=2.0       # EmailStr validation

# --- AI providers (all optional at runtime) ---
httpx>=0.27                   # talks to local Ollama
google-genai>=1.0             # Gemini (current Google Gen AI SDK)
# openai>=1.0                 # uncomment only if you use paid OpenAI

# --- Machine learning: inference runtime only ---
scikit-learn>=1.4      # required to unpickle + run the trained pipeline
joblib>=1.3            # model (de)serialization

# Training-only deps (datasets, matplotlib) live in requirements-ml.txt and are
# NOT installed in the production image — the runtime never imports them.
# Dev/test tooling lives in requirements-dev.txt (pytest, ruff).
