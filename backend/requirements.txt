# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9        # required for file uploads

# --- Database ---
SQLAlchemy>=2.0,<3.0

# --- Data pipeline ---
pandas>=2.2,<3.0
numpy>=1.26,<3.0
python-dateutil>=2.9          # robust date parsing fallback

# --- Config ---
pydantic-settings>=2.0,<3.0

# --- AI providers (all optional at runtime) ---
httpx>=0.27                   # talks to local Ollama
google-generativeai>=0.7      # Gemini free tier (optional)
# openai>=1.0                 # uncomment only if you use paid OpenAI

# --- Testing ---
pytest>=8.0
